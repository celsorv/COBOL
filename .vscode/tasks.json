{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "COBOL: Compilar e Rodar",
            "type": "shell",
            "command": "clear && cobc -x -free ${file} -o /tmp/${fileBasenameNoExtension} && /tmp/${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "focus": true,
                "clear": true
            },
            "problemMatcher": []
        }
    ]
}
